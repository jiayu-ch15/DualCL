model_dir: null


headless: false

sim: ${task.sim}
env: ${task.env}

total_frames: 100_000_000_000
max_iters: -1
eval_interval: 200
save_interval: 200
seed: 0

viewer:
  resolution: [960, 720]
  # # up
  # eye: [0.0, 0.0, 6.]
  # lookat: [0.01, 0.01, 2.]
  # front
  eye: [-0.01, -0.01, 5.0]
  lookat: [0., 0., 0.7]

wandb:
  group: ${oc.select:..task.name}
  run_name: v1_6_catch0_2_random1_0_fixed3_wodistancereward
  # run_name: debug
  job_type: train
  entity: chenjy
  project: PredatorPrey
  # mode: online
  mode: disabled # set to 'disabled' when debugging locally
  run_id: 
  monitor_gym: True
  tags: 

defaults:
  - task: HideAndSeek_circle_eval
  - algo: mappo
  - _self_
