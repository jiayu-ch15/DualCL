# model_dir: /home/jiayu/DualCL/scripts/outputs/fixed1_catch20_collision100_speed10_catch0_5_v_1_3/09-29_21-47/wandb/run-20240929_214715-fnm2vd38/files/checkpoint_1704067072.pt
model_dir: null
# hydra.job.chdir: false

headless: true

sim: ${task.sim}
env: ${task.env}

total_frames: 100_000_000_000
max_iters: -1
eval_interval: -1
save_interval: 200
seed: 0

viewer:
  resolution: [960, 720]
  # up
  eye: [0.0, 0.0, 6.]
  lookat: [0.01, 0., 2.]
  # # front
  # eye: [2.5, 2.5, 4.0]
  # lookat: [0., 0., 0.7]

wandb:
  group: ${oc.select:..task.name}
  # run_name: debug
  run_name: random0to3_catch0_5_v_1_3
  job_type: train
  entity: chenjy
  project: PredatorPrey
  mode: online
  # mode: disabled # set to 'disabled' when debugging locally
  run_id: 
  monitor_gym: True
  tags: 

defaults:
  - task: HideAndSeek_circle_cl
  # - task: HideAndSeek_circle_eval
  - algo: mappo
  - _self_
