# exp
num_agents: 3
num_obstacles: 0
size_obstacle: 0.2
v_drone: 0.5
v_low: 1.3
v_high: 1.3
size: 2.0
model_dir: null
use_collision: 1

catch_radius: 0.1


headless: true

sim: ${task.sim}
env: ${task.env}

total_frames: 250_000_000_000
max_iters: -1
eval_interval: 100
save_interval: 100
seed: 0

viewer:
  eye: [7.5, 7.5, 7.5]
  lookat: [0., 0., 0.]

wandb:
  group: ${oc.select:..task.name}
  # run_name: 0obstacle_size2_speed1_8_usecollision
  run_name: debug
  job_type: train
  entity: chenjy
  project: issac_drones
  mode: online # set to 'disabled' when debugging locally
  # mode: disabled
  run_id: 
  monitor_gym: True
  tags: 

defaults:
  # - task: PlatformHover
  - task: PredatorPrey_debug
  - algo: mappo
  - _self_
