# exp
num_agents: 3
num_obstacles: 5
obstacle_size: 0.05
v_drone: 0.5
v_low: 1.3
v_high: 1.3
v_obstacle_min: 1.0
v_obstacle_max: 1.0
size_min: 0.5
size_max: 0.5
# model_dir: /root/OmniDrones/scripts/outputs/2obstacle_size07_speed3_newmodel_loadspeed2_5/09-09_02-31/wandb/run-20230909_023139-z2wpf6mx/files/checkpoint_4561698816.pt
model_dir: null
use_collision: 1
use_speed_penalty: 1
catch_radius: 0.15
collision_radius: 0.01

headless: true

sim: ${task.sim}
env: ${task.env}

total_frames: 250_000_000_000
max_iters: -1
eval_interval: 100
save_interval: 100
seed: 0

viewer:
  eye: [2, 2, 2]
  lookat: [0., 0., 0.]

wandb:
  group: ${oc.select:..task.name}
  run_name: 5obstacle_size05_speed1_3_obstaclespeed1
  # run_name: model_debug
  job_type: train
  entity: chenjy
  project: issac_drones
  mode: online # set to 'disabled' when debugging locally
  # mode: disabled
  run_id: 
  monitor_gym: True
  tags: 

defaults:
  # - task: PlatformHover
  - task: PredatorPrey_debug
  - algo: mappo
  - _self_
